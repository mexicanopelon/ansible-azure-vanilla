- name: Add a subnet in the VNET
  azure.azcollection.azure_rm_subnet:
    name: "{{ subnet-name }}"
    resource_group: "{{ resource-group }}"
    address_prefix: "{{ addr-prefix }}"
    virtual_network: "{{ vnet }}"
  register: subnet
- debug:
    var: subnet